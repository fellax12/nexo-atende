name: Build Nexo APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Restaurar projeto Nexo 0.1.3
        shell: bash
        run: |
          cat > /tmp/nexo.b64 <<'NEXO_EOF'
          
